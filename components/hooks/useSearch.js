"use client";
import { useState, useEffect, useRef } from "react";
import { createClient } from "../../lib/supabase.js";
export function useSearch(query,token,userId){const[results,setResults]=useState(null);const[loading,setLoading]=useState(false);const debRef=useRef(null);useEffect(()=>{clearTimeout(debRef.current);const q=query.trim();if(!q||q.length<2){setResults(null);return;}debRef.current=setTimeout(()=>run(q),200);return()=>clearTimeout(debRef.current);},[query,token,userId]);async function run(q){if(!token||!userId)return;setLoading(true);try{const sb=createClient();await sb.auth.setSession({access_token:token,refresh_token:""});const types=["journal","tasks","meals","workouts"];const rows=await Promise.all(types.map(t=>sb.from("entries").select("date,data,type").eq("user_id",userId).eq("type",t).order("date",{ascending:false}).limit(400).then(r=>r.data||[])));const[notesR,tasksR,mealsR,activityR]=rows;const qL=q.toLowerCase();const hits=[];notesR.forEach(row=>{(typeof row.data==="string"?row.data:"").split("\n").filter(l=>l.trim()).forEach(line=>{if(line.toLowerCase().includes(qL))hits.push({type:"journal",date:row.date,text:line});});});tasksR.forEach(row=>{(Array.isArray(row.data)?row.data:[]).forEach(task=>{if(task?.text?.toLowerCase().includes(qL))hits.push({type:"task",date:row.date,text:task.text,done:task.done});});});mealsR.forEach(row=>{(Array.isArray(row.data)?row.data:[]).forEach(meal=>{if(meal?.text?.toLowerCase().includes(qL))hits.push({type:"meal",date:row.date,text:meal.text,kcal:meal.kcal});});});activityR.forEach(row=>{(Array.isArray(row.data)?row.data:[]).forEach(w=>{const t=w?.name||w?.text||"";if(t.toLowerCase().includes(qL))hits.push({type:"workouts",date:row.date,text:t});});});hits.sort((a,b)=>b.date.localeCompare(a.date));setResults(hits.slice(0,80));}catch(e){setResults([]);}setLoading(false);}return{results,loading};}
